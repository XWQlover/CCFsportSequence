模型定义放model文件夹，不同的模型放不同的目录 如 ./model/LR
所有的工具类放utils文件夹，数据处理类，后处理类等等
训练文件放train
每次提交前一定要保留权重，一定要保留模型权重！！！不然算作弊！！权重放weights
ensemble 存放集成的模型权重和模型集成代码

统一使用 tensorflow 1.13以上的版本
文件名全部小写，不同单词用下划线隔开
类名，文件名全部双峰
全局变量全部大写
方法，局部变量全部小写，不同单词下划线隔开